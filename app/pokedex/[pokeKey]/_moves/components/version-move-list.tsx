import React from 'react';
import type { VersionMove } from '@/app/models/detail.type';

interface VersionMoveList {
  versionMoveList: VersionMove;
}

export default function VersionMoveList({
  versionMoveList,
}: VersionMove) {
  
}